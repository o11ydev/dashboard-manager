name: go
on: [push]
jobs:
    test:
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-go@v2
        - run: go test
    build:
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-go@v2
        - run: go build
